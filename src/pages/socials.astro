---
import BaseLayout from "../layouts/BaseLayout.astro";

const links = [
    { label: "GitHub", url: "https://github.com/unlikeneptunev" },
    { label: "Instagram", url: "https://instagram.com/amdthfz" },
];
---

<BaseLayout title="socials">
    <div class="page-wrapper">
        <div class="container">
            <a href="/" class="back-link">← home</a>
            <h1 class="page-title">socials</h1>
            <ul class="social-list">
                {
                    links.map((link) => (
                        <li>
                            <a
                                href={link.url}
                                target="_blank"
                                rel="noopener noreferrer"
                                class="social-link"
                            >
                                {link.label}
                            </a>
                        </li>
                    ))
                }
            </ul>
        </div>
    </div>
</BaseLayout>

<style>
    .social-list {
        list-style: none;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        gap: 0.5rem;
    }

    .social-link {
        font-size: var(--size-xl);
        font-weight: 500;
        color: var(--text-secondary);
        transition: color 0.15s;
    }

    .social-link:hover {
        color: var(--accent-hover);
    }

    .page-title {
        font-size: var(--size-xl);
        font-weight: 700;
        color: var(--text-primary);
        margin-bottom: 3rem;
    }
</style>
